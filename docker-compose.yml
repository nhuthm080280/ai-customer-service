version: "3.8"

services:
  db:
    image: postgres:15-alpine
    container_name: aicustomer_db
    environment:
      POSTGRES_DB: aicustomer
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
#
#  adminer:
#    image: adminer:latest
#    container_name: aicustomer_adminer
#    ports:
#      - "8081:8080"
#    restart: unless-stopped
#
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    image: aicustomerservice:latest
#    container_name: aicustomerservice_app
#    ports:
#      - "8080:8080"
#    environment:
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/aicustomer
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=password
#    depends_on:
#      - db
#    restart: on-failure

volumes:
  db-data: